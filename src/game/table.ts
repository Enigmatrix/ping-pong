import { Mesh, Scene, JSONLoader, Geometry, Material } from "THREE";

export default class Table extends Mesh {
    constructor(geometry: Geometry, materials: Material, scene: Scene){
        
        super()
    }
}